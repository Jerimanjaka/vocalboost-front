import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.audiorecorder.app',
  appName: 'audio-recorder-app',
  webDir: 'dist/audio-recorder-app/browser'
};

export default config;
